const config = {
  defaultPath: '/dashboard',
  fontFamily: `Instrument Sans, sans-serif`,
  i18n: 'en',
  miniDrawer: false,
  container: true,
  mode: 'dark',
  presetColor: 'default',
  themeDirection: 'ltr'
};

export default config;
